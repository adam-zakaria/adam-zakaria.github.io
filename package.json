{
  "name": "github_page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:aws_dev": "vite --mode aws_dev",
    "prod": "npm run build-css",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-css": "sass style.scss style.css && npx tailwindcss -i style.css -o style.css && g 'save changes' && git push"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "sass": "^1.82.0",
    "tailwindcss": "^3.4.16",
    "vite": "^6.0.1"
  }
}
